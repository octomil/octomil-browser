{
  "name": "octomil-browser-sentiment-demo",
  "version": "0.0.1",
  "private": true,
  "description": "Runnable sentiment classification demo using @octomil/browser",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@octomil/browser": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
